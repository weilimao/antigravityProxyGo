### v1.3.1 更新日志

- **上游 OpenAI 兼容模型端点自适应拉取 (Model Fetch)**：
  - 新增上游候选模型端点生成与 404/405 自动续试探测能力，解决第三方聚合站与官方兼容端点列取模型失败问题；
  - 支持已知兼容子路径（如 `/api/claudecode`, `/anthropic`, `/v1` 等）智能剥离与根域端点回退探测。

- **Antigravity 直连出站用量统计闭环与防双计机制**：
  - 新增 `google_usage` 独立记账模块，补齐 `handleV1Internal` 直连出站五落点用量落库（中继用户、账号维度、全局标量、号池子聚合与请求日志）；
  - 建立严格的防双计拦截机制，保障经由 18443 本地代理出站与直连出站的数据隔离与统计精确性。

- **Anthropic 协议模型映射与上下文窗口扩展**：
  - Models API 响应中支持根据模型映射配置动态透出 `max_input_tokens` 字段，提升 Claude Code 等客户端的上下文自适应体验。

- **多池统计池化复用与单测隔离增强**：
  - 引入 `stats_pool` 池化复用机制，大幅优化高频统计指标序列化与内存分配效率；
  - 增加统计数据历史回填与 Tab 切换数据隔离等自动化单测保障。

- **前端交互与命中率计算优化**：
  - 修复仪表盘缓存命中率按号池筛选计算逻辑；
  - 优化账号配置模态框交互与 API Key 显隐状态控制。
