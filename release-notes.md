### v1.1.18 更新日志

- **新增自定义消息前缀 (PromptPrefix) 功能**：在代理客户端“设置”中新增自定义消息前缀配置卡片，支持配置多行前缀文本。开启后，在向流式聊天接口（如 Chat 窗口）发送请求时，可在最新的用户消息内容最前端自动添加指定提示词前缀。适用于快速指定模型预设、注入通用补充提示或强制约束输出格式，且不会干扰后台代码自动补全逻辑。
- **完善相关的自动化测试**：新增针对提示词前缀数据包注入及配置管理的 Go 单元测试，确保其能够兼容标准 `contents` 结构与特殊的 `v1internal` 嵌套结构，提升逻辑的异常鲁棒性。
